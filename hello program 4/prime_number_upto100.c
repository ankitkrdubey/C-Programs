#include<stdio.h>
int main()
{
    int a,b,c;
    for(a=1;a<=100;a++)
    {
        for(b=2;b<=(a/2);b++)
        {
            if(a%b==0)
            c==1;

        }
        if(c==0)
        printf("%d\t",a);
        c=0;
    }
}