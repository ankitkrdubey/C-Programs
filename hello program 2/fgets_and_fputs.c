#include<stdio.h>
int main()
{
    char str1[50],str2[50];
    puts("Enter strings...");
    gets(str1);
    puts(str1);
    puts("Enter second strings...");
    gets(str2);
    puts(str2);
    return 0;



}