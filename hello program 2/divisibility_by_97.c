#include<stdio.h>
int main()
{
    int n;
    printf("Enter the number\n");
    scanf("%d",&n);
    if(n%97==0)
    printf("number is divisible");
    else
    printf("number is not divisible");
}